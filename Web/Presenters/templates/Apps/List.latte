{extends "../@listView.latte"}

{block title}
    {_apps}
{/block}

{block header}
    {_apps}

    <div n:if="$act === 'dev' || $act === 'installed'" style="float: right;">
        <span><b>
            <a href="/editapp?act=create">{_create}</a>
        </b></span>
    </div>
{/block}

{block tabs}
    <div n:attr="id => ($act === 'catalog' ? 'activetabs' : 'ki')" class="tab">
        <a n:attr="id => ($act === 'catalog' ? 'act_tab_a' : 'ki')" href="/apps">{_apps_catalog}</a>
    </div>

    <div n:attr="id => ($act === 'installed' ? 'activetabs' : 'ki')" class="tab">
        <a n:attr="id => ($act === 'installed' ? 'act_tab_a' : 'ki')" href="?act=installed">{_installed_apps}</a>
    </div>

    <div n:attr="id => ($act === 'soft' ? 'activetabs' : 'ki')" class="tab">
        <a n:attr="id => ($act === 'soft' ? 'act_tab_a' : 'ki')" href="?act=soft">{_apps_soft}</a>
    </div>

    <div n:attr="id => ($act === 'dev' ? 'activetabs' : 'ki')" class="tab">
        <a n:attr="id => ($act === 'dev' ? 'act_tab_a' : 'ki')" href="?act=dev">{_own_apps}</a>
    </div>
{/block}

{* BEGIN ELEMENTS DESCRIPTION *}

{block link|strip|stripHtml}
    /app{$x->getId()}
{/block}

{block preview}
    <img style="max-width: 75px;" src="{$x->getAvatarUrl()}" />
{/block}

{block name}
    {$x->getName()}
    {if $x->isGame()}
        <span style="color:#777;font-weight:normal;font-size:11px;"> · {_apps_type_game}</span>
    {else}
        <span style="color:#777;font-weight:normal;font-size:11px;"> · {_apps_type_app}</span>
    {/if}
{/block}

{block description}
    {$x->getDescription()}
{/block}

{block actions}
    <a href="/app{$x->getId()}" class="profile_link">{_app_play}</a>
    <a n:if="$x->isInstalledBy($thisUser)" href="/apps/uninstall?hash={rawurlencode($csrfToken)}&app={$x->getId()}" class="profile_link">{_app_uninstall}</a>
    <a n:if="$thisUser->getId() == $x->getOwner()->getId()" href="/editapp?app={$x->getId()}" class="profile_link">{_app_edit}</a>
{/block}
